<!doctype html>
<html lang="fr">
<head>
  <%- include('partials/head', { title: 'Connexion — Private Member' }) %>
</head>
<body class="auth-body">
  <header class="auth-header">
    <div class="auth-header-brand">
      <span class="brand-mark small">PM</span>
      <span class="brand-name">Private Member</span>
    </div>
    <%- include('partials/theme-toggle') %>
  </header>

  <div class="auth-center">
    <div class="auth-card">
      <span class="brand-mark">PM</span>
      <h2>Connexion</h2>
      <p class="auth-sub">Accédez à votre espace administrateur ou personnel.</p>

      <% if (flash) { %>
        <div class="flash flash-<%= flash.type %>"><%= flash.message %></div>
      <% } %>

      <form method="POST" action="/connexion" class="auth-form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <label>
          <span>Email</span>
          <input type="email" name="email" placeholder="vous@entreprise.com" required autofocus autocomplete="username" />
        </label>
        <label>
          <span>Mot de passe</span>
          <input type="password" name="password" placeholder="••••••••" required autocomplete="current-password" />
        </label>
        <button type="submit" class="btn btn-gold btn-block">Se connecter</button>
      </form>
    </div>
    <p class="auth-footnote">Accès réservé au personnel autorisé. Les connexions sont journalisées et protégées contre les tentatives répétées.</p>
  </div>

  <script src="/js/theme.js"></script>
</body>
</html>
